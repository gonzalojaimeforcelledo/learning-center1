import {createRouter, createWebHistory} from 'vue-router';

const router=[

];

const router=createRouter({
    history: cretaeWebHistory(import.meta.env.BASE.URL),
    router:routes
});

export default router;